# Plugins Layer

## Responsibility
Loads dynamic shared library files (.dylib, .dll, .so) in the background and resolves hook handlers.

## Allowed Dependencies
- `core/contracts`
- `shared`

## Forbidden Dependencies
- Presentation UI layouts
- Database connection details
